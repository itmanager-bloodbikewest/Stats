[build]
  command = "npm run build"
  publish = "dist"

# React Router uses client-side routing (BrowserRouter), so any path
# that isn't a real static file needs to fall through to index.html.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
